<h1><%= @event.name %> </h1>
<%= @event.name %>'s attendees:<br>
<ul>
  <% @users.each do |user| %>
    <li><%= user.name %></li>
  <% end %>
</ul>
<strong><%= link_to 'Invite Guests', new_event_invite_path(@event) %></strong><br>
<br>
<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Delete', event_path, :confirm => 'Are you sure you want to delete this event?', :method => :delete %> |
<%= link_to 'Back', events_path %>